<form id="document-create" autocomplete="off">
    
    <div class="form-group">
        <label>{{localize "GDSA.system.name"}}</label>
        <div class="form-fields">
            <input id="itemCreOptName" type="text" name="name" placeholder="{{defaultName}}" value="{{name}}" autofocus>
        </div>
    </div>

    {{#if hasTypes}}
        <div class="form-group">
            <label>{{localize "GDSA.system.type"}}</label>
            <div class="form-fields">
                <select class="itemCreOpt" name="type">                 
                    {{#each types as |name type|}}
                        <option value="{{name.value}}">{{localize (concat "GDSA.itemtypes." name.value "")}}</option>
                    {{/each}}
                </select>
            </div>
        </div>
    {{/if}}

    {{#if hasFolders}}
        <div class="form-group">
            <label>{{localize "GDSA.system.folder"}}</label>
            <div class="form-fields">
                <select class="itemCreOpt" name="folder">
                    {{selectOptions folders selected=folder blank="" valueAttr="id" labelAttr="name"}}
                </select>
            </div>
        </div>
    {{/if}}

    {{{content}}}
</form>
