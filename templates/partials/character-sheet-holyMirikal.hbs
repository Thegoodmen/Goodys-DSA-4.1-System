<div class="menuHead">
    <div class="menuLine">
        Template der Gottheit

        <select class="SelHelper1 changeMirTemp" name="system.mirTemp">
            {{#select system.mirTemp}}
                {{#each config.mirTemp as |name type|}}
                    <option value="{{type}}">{{localize name}}</option>
                {{/each}}
            {{/select}}
        </select>

    </div>
    
</div>