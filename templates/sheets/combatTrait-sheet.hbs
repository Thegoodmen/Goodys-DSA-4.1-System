<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <div style="float: left; width: 373px;"><input name="name" type="text" value="{{item.name}}" style="float: left; width: 375px;" /></div>
    </header>
</form>